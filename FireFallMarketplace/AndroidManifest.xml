<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.firefallmarketplace"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".splashscreen.LogoEhrid" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".splashscreen.LogoFireFall" />
        <activity android:name=".activities.ResourceListActivity" />
        <activity android:name=".activities.CategoryListActivity" />
        <activity
            android:name=".dialog.ResourceItemDialog"
            android:theme="@style/transparentBackground" />
    </application>

</manifest>
